# Ignore styles generated by prestart script
# These styles are "exposed" & can be loaded by a host shell in order to
# provide needed styles for this app
# In a Micro Frontend environment the styles of the remote app are not loaded, since
# its index.html is not loaded.
styles.css
